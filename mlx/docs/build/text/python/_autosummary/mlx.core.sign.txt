mlx.core.sign
*************

sign(a: array, /, *, stream: None | Stream | Device = None) -> array

   Element-wise sign.

   Parameters:
      **a** (*array*) -- Input array.

   Returns:
      The sign of "a".

   Return type:
      *array*
