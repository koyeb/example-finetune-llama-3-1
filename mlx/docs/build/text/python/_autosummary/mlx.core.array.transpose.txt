mlx.core.array.transpose
************************

array.transpose(self, *axes, stream: Stream | Device | None = None) -> array

   Equivalent to "transpose()" but the axes can be passed either as a
   tuple or as separate arguments.

   See "transpose()" for full documentation.
